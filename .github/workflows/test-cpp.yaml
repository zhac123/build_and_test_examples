name: test-cpp
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ls
      - run: cd cpp/
      - run: ls
      - run: ./set-paths.sh
      - run: make fibonacci
      - run: ./fibonacci 30
